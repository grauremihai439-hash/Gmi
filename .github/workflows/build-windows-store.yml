name: Build Microsoft Store package

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

permissions:
  contents: read

jobs:
  appx:
    runs-on: windows-2025
    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Install dependencies
        run: npm ci

      - name: Type-check
        run: npm run typecheck

      - name: Require production API URL
        shell: pwsh
        run: |
          if ([string]::IsNullOrWhiteSpace("${{ vars.API_BASE_URL }}")) {
            throw "Set the API_BASE_URL repository variable to the public HTTPS API URL before packaging."
          }

      - name: Build Microsoft Store entitlement bridge
        shell: pwsh
        run: |
          dotnet publish apps/store-bridge/AIChatbot.StoreBridge.csproj `
            --configuration Release `
            --runtime win-x64 `
            --self-contained true `
            -p:PublishSingleFile=true `
            --output apps/desktop/electron/store-bridge

      - name: Build x64 APPX
        run: npm run package:windows
        env:
          VITE_API_BASE_URL: ${{ vars.API_BASE_URL }}

      - name: Upload Store package
        uses: actions/upload-artifact@v4
        with:
          name: ai-chatbot-ask-ai-anything-store-x64
          path: apps/desktop/release/*.appx
          if-no-files-found: error
          retention-days: 30
